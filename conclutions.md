## 📝 Висновки та Резюме Проекту

У рамках цього навчального проекту було успішно відтворено типовий робочий процес дата-аналітика, застосовуючи повний цикл **ETL (Extract, Transform, Load)** до датасету UK-500. Проект включав очищення, стандартизацію, трансформацію даних та базовий агрегаційний аналіз, що продемонструвало навички роботи з бібліотекою Pandas.

***

### 1. Що Було Зроблено (Огляд Кроків)

* **Очищення та Стандартизація:** Всі текстові поля (`email`, `web`) були приведені до **нижнього регістру**. Стовпці телефонів (`phone1`, `phone2`) очищені від усіх нецифрових символів за допомогою регулярних виразів.
* **Трансформація (Створення Нових Ознак):** Створено ключові ознаки, включаючи `full_name`, `email_domain` та булевий індикатор `is_gmail` для швидкої сегментації.
* **Фільтрація та Вибірки:** Проведено фільтрацію за логічними умовами (`LLC`/`Ltd` компанії, користувачі `gmail.com`, компанії з назвою ≥ 4 слів, використовуючи `df.loc[]` та `df.apply()`).
* **Агрегація та Статистика:** Розраховано кількість людей у кожному місті, визначено **ТОП-5 міст** та **ТОП-5 email-доменів**, а також загальну кількість унікальних доменів.
* **Експорт:** Фінальний очищений датасет (`uk500_clean.csv`), підвибірку користувачів Gmail (`gmail_users.csv`) та агреговані результати (`stats.xlsx` на двох вкладках) експортовано у необхідних форматах.

***

### 2. Знайдені Проблеми в Датасеті

Під час етапу **Data Cleaning** було виявлено типові проблеми, які потребували вирішення для забезпечення якості даних:

* **Незбалансований Регістр:** Поля `email` та `web` містили дані у змішаному регістрі, що могло призвести до невірного підрахунку унікальних значень (наприклад, `Google.com` vs. `google.com`).
* **Нерегулярний Формат Телефонних Номерів:** У стовпцях `phone1`/`phone2` були присутні різноманітні символи-роздільники, що ускладнювало уніфіковане зберігання та порівняння.
* **Відсутність Готових Ключових Ознак:** Для аналізу за провайдерами (доменами) ця інформація була закодована у полі `email` і вимагала **Feature Engineering**.

***

### 3. Найкорисніші Трансформації

Найбільш значущими для підвищення аналітичної цінності даних були:

1.  **Створення `email_domain`:** Це дозволило перейти від аналізу окремих адрес до статистики **провайдерів електронної пошти** (наприклад, визначення частки користувачів Gmail або Ukr.net).
2.  **Очищення Телефонних Номерів:** Уніфікація телефонів до чисто цифрового формату (`+38 097 111 22 33` $\rightarrow$ `380971112233`) є необхідною умовою для їхнього коректного використання у будь-яких системах або для валідації.
3.  **Створення `full_name`:** Об'єднання імені та прізвища забезпечило зручну ідентифікацію записів.

***

### 4. Цікаві Вибірки та Висновки

* **Лояльність до Gmail (Вибірка):** Вибірка `gmail_users.csv` та агрегація **ТОП-5 доменів** підтвердила високу концентрацію користувачів на глобальних email-платформах.
* **Юридичний Статус Компаній:** Фільтрація за термінами `LLC` та `Ltd` (регістронезалежно) дозволила швидко ідентифікувати комерційні структури, що є важливим для B2B-аналізу.
* **Географічна Концентрація:** Аналіз **ТОП-5 міст** виявив регіони з найбільшою щільністю профілів у цьому датасеті, що є основою для будь-якого регіонального фокусування.

***

### 5. Що Можна Покращити у Майбутньому

Для подальшої оптимізації та поглибленого аналізу рекомендується:

1.  **Розширена Стандартизація Адрес:** Провести нормалізацію скорочень у колонці `address` (наприклад, заміна `St` на `Street`, `Rd` на `Road`) для забезпечення єдиного формату при геокодуванні або об'єднанні з іншими базами даних.
2.  **Перевірка Валідності E-mail:** Впровадити більш сувору перевірку синтаксису email-адрес (крім наявності `@` та `.` ) для відфільтровування недійсних або помилкових записів.
3.  **Обробка Пропущених Значень:** Провести детальний аналіз пропущених значень (хоча `isna().sum()` показав їхню відсутність у цьому датасеті), що є обов'язковим кроком для більшості реальних наборів даних.
4.  **Валідація Індексів/Міст:** Перевірити відповідність поштових індексів (`postal`) до вказаних міст (`city`), щоб виявити можливі помилки у введенні географічних даних.